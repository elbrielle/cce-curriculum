# 1SW Wk3 classroom decks: build note (2026-09-15)

Five teacher decks built with the new `cce-slide-decks` skill as the proof of concept for CCE slides: `docs/resources/slides/1sw-wk3-day{1..5}.pptx`, public twins under `docs/resources/slides/public/`. Source `build/slides/1sw-wk3/day{1..5}.js`; assets (FYF pp. 28-33 and 38 crops at 170 dpi, Paws & Claws captures, BLS OOH captures, Canvas button renders, Xello lesson-guide crop) under `build/slides/1sw-wk3/assets/`. Every deck passes `qa_deck.py`. Publication to Canvas/Drive/fleet is held for owner approval (`publish_drive.sh`, `publish_week.py`).

## Gaps found while building (not fixed in the decks)

1. **Day 4 evidence guide is stale against live BLS.** `wk3-emerging-careers-link-sheet.md` carries May 2024 medians and 2024-34 projections (accessed 2026-08-09). The OOH now shows 2025 medians and 2025-35 projections (Data Scientists $120,230 / 35%; Network and Computer Systems Administrators $99,130 / -4%, captured 2026-09-15). Students who click through will see different numbers than the printed guide. The deck teaches "write the year, newer wins" and the notes flag it; the guide, its PDF, and its Doc should be regenerated from the 2025 release. The Day 1 Networking Career Cards cite the same access date but print no figures, so they are unaffected.
2. **Day 1 Networking Career Cards are a worksheet with no Doc button.** The card set ends with a "Compare the roles" response section students write in, but it is `kind: reference` in the worksheet registry and reaches students as an inline link, not a button. Per the one-button-per-response-artifact rule it needs a Doc and a button.
3. **Day 3 wireframe template reaches students as an inline link**, not a button, though it is a worksheet Doc in the registry. Same rule.
4. **Overview claims the Day 1 cards carry medians and growth**; the card source does not. Overview text should drop that clause or the cards should gain dated figures.
5. **Day 5 Canvas page says "20-question quiz"; the plan says 20-minute quiz.** Not corroborated anywhere; the deck says 20 minutes.
6. **Route registry titles for Day 2 and Day 5 exit tickets are historical** ("Website Design (Part 1)", "Mini-Presentations + Xello Skills"); registry is flagged `review_status: historical`. Day 5 has no exit ticket in the current plan; the stale Day 5 PDF should be retired.
7. **Day 4 Canvas page names "Cloud Solutions Architect" and "UX/UI Designer"** where the plan and guide say "Cloud Architect" and "UX Designer". Decks use the guide's names.
8. **No student-login Xello screenshot of About Me > Learning Style** exists in the repo; Day 5 slide 4 uses the Home-menu capture from the Xello teacher slides and the notes say to swap in a live capture when a demo login is available.
9. **Wk3 module is unpublished in 98060**, so Student View captures were impossible; button renders come from the page bodies via `crop.py fetch`. Good enough for orientation; recapture in Student View once the week is published if a modules-navigation slide is ever wanted.

## Published (2026-09-15, after owner approval)

- Drive: `SW1 · Wk3 Computer Science IT/Google Masters/` holds five native Google Slides (converted by rclone `--drive-import-formats pptx`); `Download Releases/` holds the .pptx. IDs in `build/slides/slides_registry.json`.
- Canvas 98060: .pptx uploaded to `Slides/1sw-wk3/`; each Wk3 facilitator guide's routes panel carries "Slides for Day N: Google Slides · PowerPoint (.pptx)". Page bodies only; Wk3 stays unpublished.
- Fleet (97247, 97981, 97931, 97813, 98637): same five .pptx uploaded, same five guide bodies written (plus the Wk2 Day 4 Classic Maze pages and the previously allow-listed stale pages). `fleet_parity_apply.norm_panel` now ignores Canvas link rewrites (verifier tokens, data-api attributes), which had made every panel look edited.
- Public site: `publication-policy.json` protects every .pptx by extension, so the static site gets the rights-clean twin as a PDF (`docs/resources/slides/public/1sw-wk3-dayN.pdf`) linked from each day plan above the warm-up. The .pptx twins stay in the repo. Letting .pptx through the public gate is a policy decision for the owner, not something the build should quietly change.
- Commons: not updated this run (Canvas browser session expired); update the shared resource from 98060 when convenient.
